import { ref } from 'vue';

// 火速帮 HelloWorld 示例
div({ class: 'hello-world' }) {
  
  const message = ref('你好，火速帮！')
  let count = ref(0)

  // 标题
  h1({ class: 'title' }) { '🔥 火速帮' }
  
  // 欢迎信息
  h2({ class: 'subtitle' }) { message.value }
  
  // 描述
  p({ class: 'description' }) {
    '这是一个使用 '
    a({ href: 'https://vite.dev/', target: '_blank' }) { 'Vite' }
    ' + '
    a({ href: 'https://vuejs.org/', target: '_blank' }) { 'Vue 3' }
    ' + '
    a({ href: 'https://github.com/alamhubb/ovsjs', target: '_blank' }) { 'OVS' }
    ' 构建的示例项目。'
  }

  // 计数器
  div({ class: 'counter' }) {
    button({ 
      class: 'btn',
      onClick() { count.value-- }
    }) { '-' }
    
    span({ class: 'count' }) { count }
    
    button({ 
      class: 'btn',
      onClick() { count.value++ }
    }) { '+' }
  }

  // 重置按钮
  button({ 
    class: 'btn reset',
    onClick() { count.value = 0 }
  }) { '重置' }
}
