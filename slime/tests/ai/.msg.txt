✅ Export from修复完成

**修改文件：**
- `packages/slime-parser/src/language/SlimeCstToAstUtil.ts:515-525`

**修改内容：**
```typescript
// 修改前
} else if (first1.name === Es6Parser.prototype.ExportClauseFromClauseEmptySemicolon.name) {
  const source = first1.children.find(...)
  if (source) {
    const fromClause = this.createFromClauseAst(source)
    return SlimeAstUtil.createExportNamedDeclaration(token, null, [], fromClause.source, cst.loc)
    //                                                          ↑↑ 空数组
  }
}

// 修改后
} else if (first1.name === Es6Parser.prototype.ExportClauseFromClauseEmptySemicolon.name) {
  const exportClause = first1.children.find(ch => 
    ch.name === Es6Parser.prototype.ExportClause.name)
  const source = first1.children.find(ch => ch.name === Es6Parser.prototype.FromClause.name)
  
  const specifiers = exportClause ? this.createExportClauseAst(exportClause) : []
  //                                  ↑↑ 解析ExportClause得到specifiers
  
  if (source) {
    const fromClause = this.createFromClauseAst(source)
    return SlimeAstUtil.createExportNamedDeclaration(token, null, specifiers, fromClause.source, cst.loc)
  }
}
```

**测试结果：**
✅ 108-module-export-from.js - 通过
- 输入：`export {name, age} from './user.js'`
- 输出：`export {name, age} from './user.js'` ✅ 正确（之前是 `export`）

**完整修复总结：**
✅ 1. export重命名：`export {name as userName}`
✅ 2. export from：`export {name} from './module.js'`
✅ 3. 组合：`export {name as userName} from './module.js'`
