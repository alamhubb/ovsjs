// 函数调用插值
function getName() {
  return "Alice";
}

function getAge() {
  return 25;
}

const greeting = `Hello, ${getName()}!`;
const info = `Age: ${getAge()}`;
const upper = `Name: ${getName().toUpperCase()}`;
const calc = `Result: ${Math.max(10, 20)}`;






