// 表达式插值
const a = 10;
const b = 20;
const sum = `${a} + ${b} = ${a + b}`;
const product = `${a} * ${b} = ${a * b}`;
const comparison = `${a} < ${b} is ${a < b}`;
const complex = `Result: ${(a + b) * 2}`;
const nested = `Value: ${a > 5 ? "large" : "small"}`;






