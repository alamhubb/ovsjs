// 阶段1：基础语法 - 二元运算符

// 加法
var add = 1 + 2;

// 减法
var sub = 5 - 3;

// 乘法
var mul = 4 * 2;

// 除法
var div = 10 / 2;

// 取模
var mod = 10 % 3;

// 复合运算（运算符优先级）
var complex = 1 + 2 * 3 - 4 / 2;

// 括号改变优先级（重要测试）
var withParen1 = (1 + 2) * 3;      // result: 9
var withParen2 = 10 / (2 + 3);     // result: 2
var withParen3 = (5 - 2) * (3 + 1); // result: 12

// 嵌套括号
var nested = ((1 + 2) * 3) - ((4 + 5) / 3);

