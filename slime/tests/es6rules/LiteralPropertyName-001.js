/**
 * 测试规则: LiteralPropertyName
 * 来源: 从 Literal 拆分
 */

// TODO: 添加 LiteralPropertyName 的完整测试用例
// 当前从 Literal 的内容中提取

const assert = require('assert');
const { parse } = require('../../packages/slime-parser');

describe('LiteralPropertyName', () => {
  it('should parse LiteralPropertyName', () => {
    // TODO: 补充实现测试
  });
});
