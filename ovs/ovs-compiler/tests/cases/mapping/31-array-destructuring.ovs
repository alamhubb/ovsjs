// 测试31: 数组解构
// 验证: 数组解构赋值的映射

const arr = [1, 2, 3, 4, 5]
const [first, second] = arr
const [a, , c] = arr
const [x, y, ...rest] = arr

div {
  p { first }
  p { second }
  p { a }
  p { c }
  p { x }
  p { y }
}

