// 测试22: if-else if-else语句
// 验证: 多分支条件的映射

const score = 75

div {
  if (score > 90) {
    p { "Excellent" }
  } else if (score > 70) {
    p { "Good" }
  } else if (score > 60) {
    p { "Pass" }
  } else {
    p { "Fail" }
  }
}

