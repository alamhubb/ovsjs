// 测试29: 算术运算符组合
// 验证: +, -, *, %组合运算的映射

const a = 10
const b = 3
const sum = a + b
const diff = a - b
const product = a * b
const remainder = a % b
const complex = a + b * 2 - 5

div {
  p { sum }
  p { diff }
  p { product }
  p { remainder }
  p { complex }
}

