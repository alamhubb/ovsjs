// 测试20: 模板字符串（插值）
// 验证: 模板字符串插值的映射

const name = "Alice"
const age = 25
const greeting = `Hello, ${name}!`
const info = `${name} is ${age} years old`
const calc = `Sum: ${10 + 20}`

div {
  p { greeting }
  p { info }
  p { calc }
}

