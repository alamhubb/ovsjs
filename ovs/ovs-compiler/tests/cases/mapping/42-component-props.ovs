// 测试42: 组件属性传递
// 验证: props传递和使用的映射

const Card = (props) => {
  const { title, content, author } = props
  return div {
    h2 { title }
    p { content }
    span { author }
  }
}

div {
  Card({ 
    title: "Hello", 
    content: "World", 
    author: "Alice" 
  })
}

