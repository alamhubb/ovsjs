import { ref } from 'vue';
import {div,h1,h2,h3,p} from '../utils/htmlElements';
// Hello OVS - 简单示例

const appName = "Hello OVS"
const version = "1.0"

let a = 1
let b = a
let c = b

div() {
  h2 { 'Hello OVS' }
  h1({
       onClick: () => {
         console.log('clicked')
       }
     }) {

  #{
  let a = ref(0)
    const timer = setInterval(() => {
      a.value = a.value+1
      console.log(a)
    }, 1000);
  }

  a

  }
  h1 { appName }
  p { version }
}