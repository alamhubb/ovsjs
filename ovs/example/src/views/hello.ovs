import { h, ref, reactive } from 'vue'

// 创建响应式计数器
const count = ref(0)
// 使用 reactive 创建响应式样式对象
const colors = ['red','green','blue','orange','purple']
const style = reactive({ color: colors[0] })
let colorIndex = 0

setInterval(() => {
  count.value++

   colorIndex = count.value % colors.length

   style.color = colors[colorIndex]
}, 1000)

div({style: style}) {
  p { 'Hello, OVS!' }
  p { count }
  p { '颜色变化演示' }
}