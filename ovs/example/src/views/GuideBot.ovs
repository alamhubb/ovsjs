// GuideBot主界面 - 静态版本（待支持class后改为响应式）

div {
    div {
        h1 { 'GuideBot' }
        p { '我生成了一个超简单版的任务管理工具' }
        p { '哪里需要调整？' }
    }
    
    div {
        h2 { '实时预览' }
        h1 { '我的任务' }
        
        div {
            p { '完成项目需求文档' }
        }
        div {
            p { '设计页面布局（已完成）' }
        }
    }
    
    div {
        h2 { '需求文档' }
        h3 { '功能清单' }
        p { '添加任务' }
        p { '标记完成' }
    }
}
