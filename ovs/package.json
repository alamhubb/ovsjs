{
  "name": "ovs-workspace",
  "version": "0.1.9",
  "private": true,
  "type": "module",
  "description": "OVS workspace - contains ovs-runtime and ovs-compiler packages",
  "workspaces": [
    "ovs-runtime",
    "ovs-compiler"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "echo \"See ovs/tests for tests\""
  },
  "devDependencies": {
    "slime-test": "^0.1.8",
    "tsdown": "0.17.0-beta.6"
  }
}
