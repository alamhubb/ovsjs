// 测试15: 属性访问（点号）
// 验证: 链式属性访问的映射

const data = {
  user: {
    profile: {
      name: "Alice"
    }
  }
}

const name = data.user.profile.name
const items = [1, 2, 3]
const length = items.length

div {
  p { name }
  p { length }
}

