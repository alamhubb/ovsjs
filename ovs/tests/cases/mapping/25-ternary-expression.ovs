// 测试25: 三元表达式
// 验证: 三元条件运算符的映射

const age = 18
const status = age > 18 ? "Adult" : "Minor"
const score = 85
const grade = score > 90 ? "A" : score > 80 ? "B" : "C"

div {
  p { status }
  p { grade }
}

