// 测试32: 对象解构
// 验证: 对象解构赋值的映射

const obj = { name: "Alice", age: 25, city: "NYC" }
const { name, age } = obj
const { city } = obj
const { name: userName, age: userAge } = obj

div {
  p { name }
  p { age }
  p { city }
  p { userName }
  p { userAge }
}

