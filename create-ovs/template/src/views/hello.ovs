import { ref } from 'vue';
import { div, h1, p, button } from '../utils/htmlElements';

// Hello OVS - 简单示例
const appName = "Hello OVS"
const version = "1.0"

div() {
  h1({
    onClick: () => {
      console.log('clicked')
    }
  }) {
    #{
      let count = ref(0)
      const timer = setInterval(() => {
        count.value = count.value + 1
        console.log(count)
      }, 1000);
    }
    count
  }
  h1 { appName }
  p { version }
}

